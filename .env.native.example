# Build-time settings for the Android app (vite --mode native)
# Local emulator → dev server on the host machine:
VITE_API_BASE_URL=http://10.0.2.2:8787
# Public web URL used for share links from the app (set once deployed)
VITE_PUBLIC_URL=
